# Cursor Skills（符号链接）

项目 Skill 的 **canonical 目录** 为仓库根目录 [`skills/`](../skills/README.md)。

本目录通过符号链接供 Cursor IDE 发现，与 Go Agent（Gemini）注入使用同一份 `SKILL.md`：

- `datapilot-daily-ops-report` → `../../skills/datapilot-daily-ops-report`

请勿在此直接编辑；请修改 `skills/` 下文件。
